{
  "name": "expanova",
  "version": "1.0.0",
  "description": "Expanova - Innovation Infrastructure Platform",
  "type": "module",
  "scripts": {
    "dev": "cd next && yarn dev",
    "build": "cd next && yarn build",
    "start": "cd next && yarn start",
    "setup": "cd next && yarn install"
  },
  "dependencies": {
    "@types/node": "^22.5.2",
    "typescript": "^5.0.0"
  }
}